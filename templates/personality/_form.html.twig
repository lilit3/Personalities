{{ form_start(form) }}
    {{ form_widget(form) }}
 {{ form_row(form.slide) }}
    <button class="btn btn-action-2">{{ button_label|default('Save') }}</button>
<div class="clear"></div>
{{ form_end(form) }}
